{
	"hooks": {
		"PostToolUse": [
			{
				"type": "command",
				"command": "sh -c 'case \"$TOOL_INPUT_FILE_PATH\" in *.cs) dotnet format --include \"${TOOL_INPUT_FILE_PATH#$PWD/}\" --no-restore ;; esac'",
				"windows": "powershell -NoProfile -ExecutionPolicy Bypass -Command \"$p = $env:TOOL_INPUT_FILE_PATH; if ($p -and $p.EndsWith('.cs')) { $rel = Resolve-Path -LiteralPath $p -Relative; dotnet format --include $rel --no-restore }\"",
				"timeout": 60
			}
		]
	}
}